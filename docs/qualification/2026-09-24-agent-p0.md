# Agent P0 cross-engine acceptance record

- Date: 2026-09-24
- Scope: PostgreSQL + pgvector, Redis Search, Elasticsearch through one MCP
  session; business fact, two dense retrieval paths, full-text retrieval,
  provenance, bounded results, cancellation/recovery and rejected writes.
- Implementation: [runnable example and fixture recipes](../../examples/agent-p0/README.md).

## Verified in this workspace

- `go test ./examples/agent-p0 -count=1`: passed. The test checks a grounded
  business ID across three MCP responses, cancellation/recovery, and failure
  when an unrelated hit or accepted write is returned. It also validates the
  cancellation probe against the PostgreSQL read policy. These are fixture
  tests, not live database evidence.
- `go test ./...`: passed with loopback listening permitted. The default
  sandbox cannot open `httptest` loopback sockets; rerunning with that
  restriction removed passed the Elasticsearch tests.
- `go vet ./...`, shell syntax checks for the provisioning scripts, and
  `git diff --check`: passed.
- The runnable example enforces a five-minute process deadline, 10-second
  ordinary MCP call deadlines, three SQL/ES rows or Redis hits, and a
  100-millisecond cancellation probe. Its JSON report includes the connected
  server versions and source IDs. It is not a claim that a live server ran.

## Open live-build gate

No usable Docker daemon, PostgreSQL/pgvector, Redis Search, Elasticsearch
process, or configured read-only test targets were available in this workspace.
The WSL `docker` shim reported integration disabled; `docker.exe info` reported
that the Docker Desktop Linux Engine pipe does not exist. Therefore the three
fixture scripts and the cross-engine binary could not be executed against
live targets here. Optional database integration tests remain conditional on
operator-provided artifacts and test configuration; a skipped test does not
close this gate.

Close P0 only after running the commands in the example README on disposable,
pinned builds. Capture the example's JSON output, native mutation-denial
results, PostgreSQL cancellation/recovery output, exact Redis module/profile
hashes, ES build identity, and the effective target scopes. Existing engine
qualification records provide earlier independent evidence, but do not replace
this combined live run.
